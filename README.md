# Hypatia

A Wordpress theme based on _s for samryan.net.

## Deployment

Pushing to `master` auto-deploys to NearlyFreeSpeech via GitHub Actions. Requires `NFSN_USER` and `NFSN_PASSWORD` secrets configured in the repo.
